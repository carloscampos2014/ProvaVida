<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
	<application android:allowBackup="true" android:icon="@mipmap/appicon" android:roundIcon="@mipmap/appicon_round" android:supportsRtl="true">

		<!-- Reagenda alarmes após reinicialização do dispositivo -->
		<receiver
			android:name="com.enzojb.provavida.BootReceiver"
			android:exported="true">
			<intent-filter>
				<action android:name="android.intent.action.BOOT_COMPLETED" />
			</intent-filter>
		</receiver>

		<!-- Receivers de notificação local (alarmes agendados) — sem exported pois são internos -->
		<receiver
			android:name="com.enzojb.provavida.LembreteReceiver"
			android:exported="false" />
		<receiver
			android:name="com.enzojb.provavida.AvisoInatividadeReceiver"
			android:exported="false" />

		<!-- Quick Settings Tile — mantido no manifesto manual para preservar android:icon
		     (o atributo [Service] no C# não suporta android:icon diretamente) -->
		<service
			android:name="com.enzojb.provavida.CheckInTileService"
			android:exported="true"
			android:label="Enzojb Prova de Vida"
			android:icon="@mipmap/appicon"
			android:permission="android.permission.BIND_QUICK_SETTINGS_TILE">
			<intent-filter>
				<action android:name="android.service.quicksettings.action.QS_TILE" />
			</intent-filter>
		</service>

		<!-- Widgets: CheckInWidgetSimples e CheckInWidgetCompleto são registrados
		     via atributos C# [BroadcastReceiver] + [MetaData] nas classes correspondentes.
		     Declarações manuais removidas para evitar conflito no merge do build MAUI. -->

	</application>
	<uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
	<uses-permission android:name="android.permission.INTERNET" />
	<uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
	<uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
	<uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
	<uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
	<uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
</manifest>
