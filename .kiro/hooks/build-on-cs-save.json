{
  "version": "v1",
  "hooks": [
    {
      "name": "Build ao salvar arquivo C#",
      "trigger": "PostFileSave",
      "description": "Roda dotnet build silencioso ao salvar qualquer arquivo .cs do backend para detectar erros de compilação imediatamente",
      "matcher": "\\.cs$",
      "action": {
        "type": "command",
        "command": "dotnet build \"c:\\Dev\\ProvaVida\\src\\ProvaVida.Api\\ProvaVida.Api.csproj\" --no-restore -v quiet 2>&1 | Select-Object -Last 5",
        "timeout": 60
      }
    }
  ]
}
